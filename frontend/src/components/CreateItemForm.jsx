const CreateItemForm = () => {
  return (
    <div>
      <h1>Create Item</h1>
      // create a form that is taking item title,description, images, category,
      desiredItems (items that you want to swap with), visible (posted or
      unposted handled with a switch)
    </div>
  );
};

export default CreateItemForm;
